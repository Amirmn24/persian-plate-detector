# 🔄 راهنمای به‌روزرسانی به نسخه 2.0.0

این راهنما به شما کمک می‌کند تا پروژه را به نسخه جدید با قابلیت تشخیص پلاک به‌روزرسانی کنید.

## 📋 تغییرات اصلی

در نسخه 2.0.0 قابلیت‌های زیر اضافه شده است:

- ✅ تشخیص خودکار پلاک ایرانی در تصاویر
- ✅ خواندن متن پلاک با OCR فارسی
- ✅ تجزیه و نمایش اجزای پلاک (2 رقم + حرف + 3 رقم + 2 رقم)
- ✅ رسم کادر روی پلاک و نمایش متن پلاک
- ✅ ذخیره اطلاعات پلاک در دیتابیس

## 🔧 مراحل به‌روزرسانی

### 1. دریافت کد جدید

اگر از Git استفاده می‌کنید:

```cmd
git pull origin main
```

یا فایل‌های جدید را دانلود کنید.

### 2. نصب کتابخانه‌های جدید

کتابخانه‌های جدید مورد نیاز:
- `easyocr` - برای تشخیص و خواندن متن فارسی
- `torch` - نیاز EasyOCR

#### روش خودکار (ویندوز):

```cmd
pip install -r requirements.txt
```

#### روش دستی:

```cmd
pip install easyocr
pip install torch
```

### 3. به‌روزرسانی دیتابیس

migration جدید را اعمال کنید:

```cmd
python manage.py migrate
```

این دستور 3 فیلد جدید به مدل `UploadedImage` اضافه می‌کند:
- `has_plate` (Boolean)
- `plate_count` (Integer)
- `plate_data` (JSON)

### 4. دانلود مدل‌های OCR

در اولین اجرا، EasyOCR مدل‌های زبان فارسی و انگلیسی را دانلود می‌کند:

```cmd
python -c "import easyocr; reader = easyocr.Reader(['fa', 'en'])"
```

این فرآیند ممکن است 2-5 دقیقه طول بکشد (بسته به سرعت اینترنت).

### 5. تست سیستم

سرور را اجرا کنید:

```cmd
python manage.py runserver 127.0.0.1:8000
```

و یک تصویر تست آپلود کنید.

## 📦 حجم دانلود

مدل‌های جدید که دانلود می‌شوند:

- EasyOCR مدل فارسی: ~40 MB
- EasyOCR مدل انگلیسی: ~10 MB
- PyTorch (اگر نصب نباشد): ~100-700 MB

**توجه**: دانلود فقط یکبار انجام می‌شود و فایل‌ها در cache ذخیره می‌شوند.

## ⚙️ تنظیمات اختیاری

### غیرفعال کردن GPU (برای سیستم‌های بدون NVIDIA GPU)

در فایل `utlis/plate_reader.py` خط 56:

```python
self.ocr_reader = easyocr.Reader(['fa', 'en'], gpu=False)
```

`gpu=False` به صورت پیش‌فرض تنظیم شده است.

### تغییر میزان اطمینان

در فایل `detector/views.py`:

```python
plate_reader = IranianPlateReader(confidence=0.3)
```

مقدار `confidence` را می‌توانید بین 0.1 تا 0.9 تنظیم کنید:
- کمتر = تشخیص بیشتر اما احتمال خطای بیشتر
- بیشتر = دقت بالاتر اما احتمال از دست دادن پلاک‌ها

## 🐛 رفع مشکلات احتمالی

### خطا: "No module named 'easyocr'"

```cmd
pip install easyocr
```

### خطا: "No module named 'torch'"

```cmd
pip install torch
```

### خطای دانلود مدل OCR

اگر مدل‌ها دانلود نمی‌شوند:

1. اتصال اینترنت خود را بررسی کنید
2. VPN یا پروکسی را امتحان کنید
3. دستور زیر را مستقیماً اجرا کنید:

```cmd
python -c "import easyocr; reader = easyocr.Reader(['fa', 'en'])"
```

### خطای Migration

اگر خطای migration دریافت کردید:

```cmd
python manage.py makemigrations
python manage.py migrate
```

اگر همچنان مشکل دارید، می‌توانید دیتابیس را حذف و دوباره بسازید:

```cmd
del db.sqlite3
python manage.py migrate
```

**توجه**: این کار تمام داده‌های قبلی را حذف می‌کند!

## ✅ بررسی نصب موفق

بعد از به‌روزرسانی، موارد زیر باید کار کنند:

1. ✅ سرور بدون خطا اجرا شود
2. ✅ صفحه اصلی باز شود
3. ✅ آپلود تصویر کار کند
4. ✅ خودروها تشخیص داده شوند
5. ✅ پلاک‌ها شناسایی شوند (اگر در تصویر وجود داشته باشند)
6. ✅ متن پلاک خوانده شود
7. ✅ صفحه نتیجه با اطلاعات پلاک نمایش داده شود

## 📊 سازگاری با نسخه‌های قبلی

- ✅ تصاویر قدیمی بدون مشکل کار می‌کنند
- ✅ فیلدهای جدید برای رکوردهای قدیمی مقدار پیش‌فرض دارند
- ✅ امکان پردازش مجدد تصاویر قدیمی (با آپلود دوباره)

## 💡 نکات

1. **اولین بار کند است**: اولین بار که سیستم اجرا می‌شود، مدل‌ها دانلود می‌شوند و ممکن است کند باشد.

2. **حافظه RAM**: تشخیص پلاک نیاز به حافظه بیشتری دارد (~2-3 GB RAM توصیه می‌شود).

3. **کیفیت تصویر**: برای نتایج بهتر از تصاویر با کیفیت بالا استفاده کنید.

4. **زمان پردازش**: پردازش هر تصویر ممکن است 5-15 ثانیه طول بکشد (بسته به سخت‌افزار).

## 🆘 پشتیبانی

اگر مشکلی داشتید:

1. ابتدا بخش "رفع مشکلات احتمالی" را بررسی کنید
2. فایل `USAGE.md` را مطالعه کنید
3. یک Issue در GitHub باز کنید

---

**موفق باشید! 🚀**
