# 📖 راهنمای استفاده از سیستم تشخیص خودرو و پلاک

این راهنما به شما کمک می‌کند تا از تمام قابلیت‌های سیستم به بهترین شکل استفاده کنید.

## 🚀 شروع سریع

### نصب و راه‌اندازی

#### روش 1: استفاده از فایل نصب خودکار (ویندوز)
```cmd
install.bat
```

#### روش 2: نصب دستی
```cmd
pip install -r requirements.txt
python manage.py makemigrations
python manage.py migrate
```

### اجرای برنامه

#### روش 1: استفاده از فایل اجرای سریع (ویندوز)
```cmd
run.bat
```

#### روش 2: اجرای دستی
```cmd
python manage.py runserver 127.0.0.1:8000
```

سپس مرورگر خود را باز کرده و به آدرس `http://127.0.0.1:8000` بروید.

## 📸 آپلود و تشخیص

### مراحل آپلود تصویر

1. در صفحه اصلی، روی دکمه "برای آپلود تصویر کلیک کنید" کلیک کنید
2. یا تصویر خود را به محدوده آپلود بکشید و رها کنید (Drag & Drop)
3. پیش‌نمایش تصویر نمایش داده می‌شود
4. روی دکمه "شروع تشخیص" کلیک کنید
5. منتظر بمانید تا پردازش تصویر انجام شود

### فرمت‌های پشتیبانی شده

- JPG / JPEG
- PNG
- BMP
- WEBP

### اندازه تصویر

- حداکثر اندازه: بدون محدودیت (توصیه می‌شود کمتر از 10MB)
- رزولوشن توصیه شده: HD (1280x720) یا بالاتر

## 🚗 نحوه تشخیص خودرو

سیستم می‌تواند انواع خودرو را تشخیص دهد:

- 🚗 **ماشین** (Car) - رنگ سبز
- 🏍️ **موتورسیکلت** (Motorcycle) - رنگ آبی
- 🚌 **اتوبوس** (Bus) - رنگ نارنجی
- 🚚 **کامیون** (Truck) - رنگ قرمز

هر خودرو با یک کادر رنگی مشخص می‌شود و میزان اطمینان تشخیص نمایش داده می‌شود.

## 🆔 نحوه تشخیص پلاک ایرانی

### فرمت پلاک ایرانی

پلاک ایرانی از چپ به راست به صورت زیر است:

```
[2 رقم] [حرف فارسی] [3 رقم] - [2 رقم]
```

مثال:
```
46 الف 532 - 12
```

### مراحل تشخیص پلاک

1. **تشخیص خودرو**: ابتدا خودرو در تصویر شناسایی می‌شود
2. **یافتن محل پلاک**: سیستم با استفاده از رنگ (سفید یا زرد) و شکل، محل پلاک را پیدا می‌کند
3. **خواندن متن**: با استفاده از OCR فارسی، متن پلاک خوانده می‌شود
4. **تجزیه فرمت**: متن به اجزای خود (2 رقم + حرف + 3 رقم + 2 رقم) تجزیه می‌شود

### دقت تشخیص

دقت تشخیص پلاک به عوامل زیر بستگی دارد:

✅ **عوامل مثبت**:
- کیفیت بالای تصویر
- نور مناسب و یکنواخت
- پلاک رو به دوربین
- پلاک تمیز و بدون خراش
- فاصله مناسب از دوربین

❌ **عوامل منفی**:
- تصویر تار یا کم نور
- زاویه نامناسب
- پلاک کثیف یا خراشیده
- پلاک خیلی دور یا خیلی نزدیک
- سایه روی پلاک

## 📊 صفحات سیستم

### 1. صفحه اصلی (Home)

- آپلود تصویر جدید
- مشاهده آخرین 6 تصویر پردازش شده
- نمایش تعداد خودرو و پلاک برای هر تصویر

### 2. صفحه نتیجه (Result)

پس از پردازش تصویر، به این صفحه منتقل می‌شوید:

**آمار کلی**:
- وضعیت تشخیص خودرو (موفق/ناموفق)
- تعداد خودروهای شناسایی شده
- تعداد پلاک‌های شناسایی شده
- تاریخ و زمان پردازش

**تصاویر**:
- تصویر اصلی
- تصویر پردازش شده (با کادرهای خودرو و پلاک)

**جزئیات خودروها**:
جدولی شامل:
- ردیف
- نوع خودرو
- میزان اطمینان (به صورت نوار پیشرفت)
- شماره پلاک (در صورت شناسایی)
- موقعیت در تصویر

**جزئیات پلاک‌ها**:
برای هر پلاک شناسایی شده:
- شماره پلاک کامل
- تجزیه به اجزا (2 رقم + حرف + 3 رقم + 2 رقم)
- متن کامل خوانده شده
- میزان اطمینان

### 3. صفحه تاریخچه (History)

- لیست تمام تصاویر پردازش شده
- امکان جستجو و فیلتر
- نمایش اطلاعات پلاک در لیست
- دکمه مشاهده جزئیات برای هر تصویر

## 🔧 تنظیمات پیشرفته

### تنظیم میزان اطمینان

در فایل `detector/views.py` می‌توانید میزان اطمینان را تغییر دهید:

```python
# برای تشخیص خودرو
detector = VehicleDetector(model_name="yolov8n.pt", confidence=0.5)

# برای تشخیص پلاک
plate_reader = IranianPlateReader(confidence=0.3)
```

مقادیر بین 0 تا 1:
- مقادیر کوچکتر (0.3): تشخیص بیشتر اما خطای بیشتر
- مقادیر بزرگتر (0.7): تشخیص دقیق‌تر اما کمتر

### انتخاب مدل YOLO

می‌توانید از مدل‌های مختلف YOLO استفاده کنید:

- `yolov8n.pt`: سریع‌ترین (پیش‌فرض)
- `yolov8s.pt`: متوسط
- `yolov8m.pt`: دقیق‌تر
- `yolov8l.pt`: بسیار دقیق
- `yolov8x.pt`: دقیق‌ترین

## 🐛 رفع مشکلات

### خطای "Module not found"

```cmd
pip install -r requirements.txt
```

### خطای "No module named 'easyocr'"

```cmd
pip install easyocr
```

### دانلود کند مدل‌ها

در اولین اجرا، مدل‌های زیر دانلود می‌شوند:
- YOLOv8 (~6 MB)
- EasyOCR فارسی (~40 MB)
- EasyOCR انگلیسی (~10 MB)

این کار فقط یکبار انجام می‌شود.

### پلاک خوانده نمی‌شود

1. **کیفیت تصویر را بهبود دهید**: از تصویر با رزولوشن بالاتر استفاده کنید
2. **نور را بهبود دهید**: اطمینان حاصل کنید پلاک به خوبی روشن است
3. **زاویه را تغییر دهید**: پلاک باید تقریباً رو به دوربین باشد
4. **فاصله را تنظیم کنید**: پلاک نباید خیلی دور یا خیلی نزدیک باشد

### سرعت پردازش کند است

1. از تصاویر با اندازه کوچکتر استفاده کنید
2. مدل سبک‌تر YOLO را انتخاب کنید (`yolov8n.pt`)
3. GPU را فعال کنید (نیاز به CUDA)

## 💡 نکات و ترفندها

### بهترین نتایج

1. **تصویر با نور روز**: تصاویر در نور روز بهترین نتیجه را دارند
2. **زاویه مستقیم**: تصاویری که خودرو را از جلو یا عقب نشان می‌دهند، بهتر هستند
3. **کیفیت بالا**: از دوربین‌های با کیفیت بالا استفاده کنید
4. **بدون حرکت**: تصاویر ثابت (بدون motion blur) بهتر هستند

### محدودیت‌ها

- فقط پلاک‌های ایرانی با فرمت استاندارد
- تصاویر باید خودرو را به وضوح نشان دهند
- پلاک‌های خیلی خراشیده یا کثیف ممکن است خوانده نشوند
- پلاک‌های دستکاری شده تشخیص داده نمی‌شوند

## 📞 پشتیبانی

در صورت بروز مشکل:

1. ابتدا بخش "رفع مشکلات" را مطالعه کنید
2. README.md را مطالعه کنید
3. Issues در GitHub را بررسی کنید
4. یک Issue جدید باز کنید

## 🔄 به‌روزرسانی

برای دریافت آخرین نسخه:

```cmd
git pull origin main
pip install -r requirements.txt
python manage.py migrate
```

---

**تهیه شده با ❤️ برای توسعه‌دهندگان ایرانی**
